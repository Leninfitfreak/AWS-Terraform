name: AWS Staging Destroy

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Destroy test message
        run: echo "Destroy workflow registered successfully"
